import React from 'react';

const SimpleContent = () => {
	return (
		<span>
			here comes the data that's from the request... check console ...
			shift+ctrl+i
		</span>
	);
};

export default SimpleContent;
